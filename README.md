# Arch Docker build example

This docker example builds a list of packages from `src/` and outputs the builds into `out/`.

## Requirements

* Docker

## Installation

First build the base image

```
./init.sh
```

## Usage

Then run compilation

```
./run.sh
```
